/**
 * Returns a sorted array in the ascending order.
 * @param arr The input array.
 * @returns {*[]} The sorted copy of an input array.
 */
function sortNumberArray(arr) {
    return [...arr].sort();
}